# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local env — every file that could hold a key. `.env` itself was missing:
# a local ANTHROPIC_API_KEY was one `git add -A` from the repository
# (docs/SECURITY.md, O4). The example carries no values and stays tracked.
.env
.env.*
!.env.example

# Sibling projects in this workspace — not part of the Niyyah repo
/rite
/forge
/axis
/sovereign
/affiliate-simulator

# Unrelated large file living at workspace root
/THE HIDDEN MONEY GATE.pdf

# Local dev tooling config — references sibling projects + a machine-specific path
/.claude

# Local Netlify folder
.netlify

# The Guide’s live evaluation reports — the baseline is committed, runs are not (docs/GUIDE-EVAL.md).
tests/guide-eval/results/
